# 坦克大战 - 运行说明 Tank Game - Instructions

## 📁 文件说明 / File Description

| 文件名 / Filename | 说明 / Description |
|-------------------|---------------------|
| `index.html` | 游戏主页面，包含导航栏、Canvas 画布、排行榜弹窗 / Game homepage with navbar, canvas, leaderboard modal |
| `game.js` | 游戏核心逻辑，包含地图、坦克、子弹、碰撞检测、AI、音效、游戏记录保存 / Game core logic |
| `style.css` | 游戏样式，NES 像素风格 + 响应式移动端适配 / Game styles with NES pixel style |
| `auth.css` | 认证页面样式（登录、注册、导航栏、排行榜弹窗）/ Auth page styles |
| `login.html` | 登录页面 / Login page |
| `register.html` | 注册页面 / Register page |
| `profile.html` | 个人中心页面，显示历史游戏记录 / Profile page with game history |
| `server.js` | Express 后端服务器 / Express backend server |
| `db/init.js` | 数据库初始化脚本 / Database initialization script |

---

## 🚀 快速开始 / Quick Start

### 前置条件 / Prerequisites

- Node.js 14+
- PostgreSQL 12+

### 安装步骤 / Installation Steps

1. **安装依赖 / Install Dependencies**
   ```bash
   npm install
   ```

2. **创建数据库 / Create Database**
   
   打开 PostgreSQL 命令行或使用 psql：
   
   Open PostgreSQL command line or use psql:
   ```sql
   CREATE DATABASE tank_game;
   ```

3. **初始化数据库表 / Initialize Database Tables**
   ```bash
   npm run init-db
   ```

4. **启动服务器 / Start Server**
   ```bash
   npm start
   ```

5. **访问游戏 / Access Game**
   
   打开浏览器访问 / Open browser and visit: http://localhost:3000

---

## ⚙️ 数据库配置 / Database Configuration

### 环境变量配置 / Environment Variables

Windows PowerShell:
```powershell
$env:DB_HOST="localhost"
$env:DB_PORT="5432"
$env:DB_NAME="tank_game"
$env:DB_USER="postgres"
$env:DB_PASSWORD="your_password"
npm start
```

Linux/Mac:
```bash
export DB_HOST=localhost
export DB_PORT=5432
export DB_NAME=tank_game
export DB_USER=postgres
export DB_PASSWORD=your_password
npm start
```

### 数据库表结构 / Database Schema

**users 表 / users table**
| 字段 / Field | 类型 / Type | 说明 / Description |
|--------------|-------------|---------------------|
| id | SERIAL | 主键 / Primary key |
| username | VARCHAR(50) | 用户名（唯一）/ Username (unique) |
| password | VARCHAR(255) | 密码（哈希）/ Password (hashed) |
| created_at | TIMESTAMP | 注册时间 / Registration time |

**game_records 表 / game_records table**
| 字段 / Field | 类型 / Type | 说明 / Description |
|--------------|-------------|---------------------|
| id | SERIAL | 主键 / Primary key |
| user_id | INTEGER | 关联用户 ID / Related user ID |
| scenario | VARCHAR(100) | 场景名称 / Scenario name |
| final_score | INTEGER | 最终得分 / Final score |
| result | VARCHAR(20) | 结果（通关/失败）/ Result (win/lose) |
| played_at | TIMESTAMP | 游戏时间 / Game time |

---

## 🎮 游戏功能 / Game Features

### 用户系统 / User System

- **注册 / Register**：用户名 2-50 字符，密码至少 6 位 / Username 2-50 chars, password min 6 chars
- **登录 / Login**：注册成功后自动登录 / Auto-login after registration
- **登出 / Logout**：点击"退出登录"按钮 / Click logout button

### 游戏记录 / Game Records

- 已登录用户完成游戏后自动保存记录 / Auto-save for logged-in users
- 未登录用户提示"登录后可保存你的游戏记录" / Prompt to login for saving records
- 记录内容：场景、得分、结果、时间 / Records: scenario, score, result, time

### 排行榜 / Leaderboard

- 显示全服 TOP 10 最高分排名 / Show TOP 10 highest scores
- 包含：排名、指挥官、最高分、游戏场次 / Include: rank, player, best score, games played
- 前三名显示金、银、铜色 / Top 3 shown in gold, silver, bronze

### 个人中心 / Profile Center

- 查看历史游戏记录 / View game history
- 显示场景、得分、结果、时间 / Show scenario, score, result, time

---

## 🎯 操作说明 / Controls

| 操作 / Action | 电脑端 / Desktop | 手机端 / Mobile |
|---------------|------------------|-----------------|
| 移动 / Move | `W A S D` 或 方向键 / Arrow keys | 左下方向键 / D-pad |
| 射击 / Fire | 空格键 / Space | 右下 🔥 按钮 / Fire button |
| 开始/重来 / Start/Restart | 回车 / 空格 / Enter / Space | 点击画面 / Tap screen |

---

## 🕹️ 游戏规则 / Game Rules

- 目标：保护底部的鹰标基地，消灭全部 30 辆敌方坦克获胜
- Goal: Protect the eagle base, destroy all 30 enemy tanks to win
- 玩家拥有 3 条生命，被击中后短暂无敌重生
- Player has 3 lives, brief invincibility after respawn
- 砖墙可被子弹摧毁，钢墙不可摧毁
- Brick walls can be destroyed, steel walls cannot
- 同一时间最多 12 辆敌方坦克在场
- Max 12 enemy tanks on field at once
- 基地被击中 → 直接 Game Over
- Base hit → Game Over

---

## 📱 手机局域网试玩 / Mobile LAN Play

**条件 / Prerequisite**：手机和电脑必须连接同一个 WiFi / Phone and PC must be on same WiFi

**步骤 / Steps**：

1. 启动服务器 / Start server: `npm start`

2. 查看电脑的局域网 IP / Check PC's LAN IP:
   ```powershell
   ipconfig | findstr /i "IPv4"
   ```

3. 手机打开浏览器访问 / Open browser on phone:
   ```
   http://<电脑IP>:3000
   ```

4. 手机端会自动显示虚拟方向键和开火按钮
   Mobile will auto-show virtual D-pad and fire button

---

## 🌐 互联网部署 / Internet Deployment

### GitHub Pages（仅前端 / Frontend Only）

适合仅展示游戏，不包含用户系统 / For game demo only, without user system

### Vercel / Railway（完整功能 / Full Features）

1. 推送代码到 GitHub / Push code to GitHub
2. 在 Vercel/Railway 创建项目 / Create project on Vercel/Railway
3. 配置环境变量（数据库连接）/ Configure environment variables (database)
4. 部署完成 / Deploy complete

---

## 🛠️ 常见问题 / FAQ

| 问题 / Issue | 解决办法 / Solution |
|--------------|---------------------|
| 无法连接数据库 / Cannot connect to database | 检查 PostgreSQL 是否运行，确认密码正确 / Check PostgreSQL running, verify password |
| 注册显示"服务器错误" / Registration shows "server error" | 检查数据库连接，重启服务器 / Check database connection, restart server |
| 游戏记录未保存 / Game record not saved | 确认已登录，检查控制台错误 / Confirm logged in, check console errors |
| 手机访问不到 / Cannot access from phone | 确认同 WiFi，关闭 VPN，检查防火墙 / Same WiFi, disable VPN, check firewall |
| 没有声音 / No sound | 浏览器需先点击页面才能播放声音 / Browser needs click first to play sound |

---

## 📦 依赖说明 / Dependencies

| 依赖 / Dependency | 用途 / Purpose |
|-------------------|----------------|
| express | Web 服务器 / Web server |
| express-session | 会话管理 / Session management |
| pg | PostgreSQL 客户端 / PostgreSQL client |
| bcrypt | 密码哈希加密 / Password hashing |