# 坦克大战 - 运行说明

## 📁 文件说明

| 文件名 | 说明 |
|--------|------|
| `index.html` | 游戏主页面，包含 Canvas 画布和移动端虚拟按键结构 |
| `game.js` | 游戏核心逻辑，包含地图、坦克、子弹、碰撞检测、AI、音效等全部功能 |
| `style.css` | 游戏样式，NES 像素风格 + 响应式移动端适配 |

---

## 🎮 快速开始（电脑本地运行）

### 方法一：Python 本地服务器（推荐，最稳定）

**前置条件**：电脑已安装 Python 3（命令行输入 `python --version` 确认）。

**每次运行步骤**：

1. 打开命令行（CMD 或 PowerShell），进入游戏文件夹：
   ```bat
   cd /d "d:\5 Test\Solo\Tank Game"
   ```

2. 启动本地 HTTP 服务器：
   ```bat
   python -m http.server 8080
   ```
   > 端口 8080 可自定义，被占用时换一个（如 8081）。

3. 打开浏览器，访问：
   ```
   http://localhost:8080
   ```

4. 游戏开始后，按 **回车** 或 **空格** 开始游戏。

5. 关闭：在命令行窗口按 `Ctrl + C` 停止服务器。

---

### 方法二：直接双击打开（最简，但部分浏览器可能限制音效）

1. 双击 `index.html`，用浏览器打开。

2. 若音效无法播放或页面报错，改用方法一。

---

## 📱 手机局域网试玩

**条件**：手机和电脑必须连接 **同一个 WiFi**。

**步骤**：

1. 按"方法一"启动本地服务器。

2. 在电脑命令行中执行以下命令，查看电脑的局域网 IP：
   ```bat
   ipconfig | findstr /i "IPv4"
   ```
   输出类似：
   ```
   IPv4 Address  . . . . . . . . . . : 192.168.1.7
   IPv4 Address  . . . . . . . . . . : 192.168.10.8
   ```

3. 手机打开浏览器，依次尝试：
   ```
   http://192.168.1.7:8080
   http://192.168.10.8:8080
   ```
   > 端口号要和服务器启动时的一致。能加载出画面的那个就是正确地址。

4. 手机端会自动显示虚拟方向键（左下）和开火按钮（右下），点击画面任意位置即可开始游戏。

---

## 🌐 互联网部署分享（给远程好友玩）

### 方案 A：Netlify（最快，拖拽上传）

1. 访问 <https://app.netlify.com/drop>
2. 将 `index.html`、`game.js`、`style.css` **三个文件**一起拖入页面。
3. 等待几秒即可获得临时链接（类似 `https://xxx.netlify.app`），直接发给好友即可。
4. （可选）登录后可自定义域名、永久保留链接。

### 方案 B：Vercel

1. 安装 Node.js，命令行执行：`npm i -g vercel`
2. 进入游戏目录，执行 `vercel`，按提示登录部署。
3. 获得可分享的 `xxx.vercel.app` 链接。

### 方案 C：GitHub Pages

1. 创建新仓库，将三个文件 Push 上去。
2. 仓库 Settings → Pages → Source 选 `main` 分支根目录。
3. 等待 1-2 分钟，访问 `https://<你的用户名>.github.io/<仓库名>/`

---

## 🎯 操作说明

| 操作 | 电脑端 | 手机端 |
|------|--------|--------|
| 移动 | `W A S D` 或 方向键 | 左下方向键 |
| 射击 | 空格键 | 右下 🔥 按钮 |
| 开始/重来 | 回车 / 空格 | 点击画面任意位置 |

---

## 🕹️ 游戏规则

- 目标：保护底部的 **鹰标基地**，消灭全部 **10 辆敌方坦克** 即获胜。
- 玩家拥有 **3 条生命**，被击中后短暂无敌重生。
- **砖墙** 可被子弹摧毁，**钢墙** 不可摧毁。
- 同一时间最多 **4 辆敌方坦克** 在场，击败后会从地图顶部刷新新的敌人。
- 基地被击中 → 直接 Game Over。

---

## 🛠️ 常见问题

| 问题 | 解决办法 |
|------|----------|
| 画面黑屏 / 加载不出 | 检查是否用了方法一（本地服务器），直接双击打开可能被浏览器策略拦截 |
| 坦克无法移动 | 刷新页面（Ctrl+F5），确认按了回车/空格进入了游戏（非菜单画面） |
| 手机访问不到 | 确认同 WiFi；手机开了 VPN/代理需关闭；Windows 防火墙放行 Python |
| 没有声音 | 浏览器需先有交互（点击一次画面）才能播放声音，这是浏览器策略 |
| 端口被占用 | 换一个端口，如 `python -m http.server 8081` |
